---
apiVersion: oauth.openshift.io/v1
kind: OAuthClient
metadata:
  name: {{ ui_service_name }}
  namespace: "{{ app_namespace }}"
grantMethod: auto
redirectURIs:
- "https://{{ ui_service_name }}.{{ app_name }}.{{ cluster_dns_suffix }}/login/callback"
secret: "{{ ui_oauth_secret }}"
